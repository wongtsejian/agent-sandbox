# Gateway Internals

The gateway is a transparent egress proxy that runs as a separate container alongside the agent. All outbound HTTPS from the agent routes through it via iptables DNAT (port 443 → gateway:8443). Its job is to intercept specific TLS connections for credential injection while passing everything else through untouched.

## Architecture

```
Agent outbound TCP :443 → iptables DNAT → Gateway TCP listener (:8443)
                                               │
                                               ├─ Read TLS ClientHello
                                               ├─ Extract SNI (server name)
                                               │
                                               ├─ SNI matches MITM domain?
                                               │    YES → mitm.Handler (terminate TLS, rewrite HTTP, forward)
                                               │    NO  → passthrough (pipe bytes directly to upstream)
                                               │
Gateway health endpoint (:8080/health) ← Agent entrypoint polls before setting up iptables
```

## Module Layout

```
core/gateway/
├── cmd/gateway/main.go            ← entrypoint, wiring, health endpoint, plugin loader
└── internal/
    ├── proxy/
    │   ├── config.go              ← Config structs, YAML loading
    │   ├── proxy.go               ← TCP listener, SNI routing, passthrough
    │   ├── http.go                ← Plain HTTP proxy handler
    │   ├── sni.go                 ← TLS ClientHello SNI extraction
    │   └── forward.go             ← Generic TCP port forwarder
    ├── mitm/
    │   ├── mitm.go                ← MITM handler (TLS termination + HTTP pipeline)
    │   └── cert.go                ← CA generation, on-demand cert generation, CertCache
    ├── plugin/
    │   ├── loader.go              ← Plugin discovery, esbuild bundling, VM registration
    │   ├── vm.go                  ← goja VM factory, host API injection
    │   └── hostapi.go             ← Host functions exposed to TS (setHeader, getHeader, log, etc.)
    ├── redact/
    │   └── handler.go             ← slog handler that masks secrets in logs
    └── dns/
        └── dns.go                 ← UDP DNS forwarder

core/plugins/
├── github-pat/
│   ├── plugin.yaml                ← metadata, domains, options schema
│   └── src/middleware.ts          ← GitHub PAT auth (TS runtime plugin)
└── mcp-oauth/
    ├── plugin.yaml                ← metadata, domains, options schema
    └── src/middleware.ts          ← OAuth token refresh (TS runtime plugin)
```

## Key Interfaces

### RequestHandler (proxy package)

The central routing contract. The proxy iterates registered handlers on every connection:

```go
type RequestHandler interface {
    Matches(host string) bool
    Handle(clientConn net.Conn, initialData []byte, serverName string)
}
```

### Middleware (TypeScript plugins)

All request modification flows through TypeScript plugins loaded at gateway startup. Each plugin exports a `middleware` function:

```typescript
// core/plugins/github-pat/src/middleware.ts
import type { MiddlewareContext } from "@agent-sandbox/sdk";

export function middleware(ctx: MiddlewareContext): void {
  ctx.request.setHeader("Authorization", `Bearer ${ctx.options.token}`);
}
```

Domain-scoped: each plugin only runs for requests matching its configured domains (declared in `plugin.yaml`).

## TypeScript Runtime

The gateway embeds a JavaScript runtime powered by **goja** (pure-Go ES5.1+ engine). TypeScript plugins are transpiled and bundled at startup using **esbuild** (embedded via a Go binding), producing self-contained ES5 bundles that goja can execute.

### Plugin Loading (startup sequence)

1. Gateway reads `plugins.yaml` (generated by CLI from merged plugin configs)
2. For each plugin entry:
   - Resolves the plugin directory (contains `plugin.yaml` + `src/`)
   - Calls esbuild to bundle `src/middleware.ts` → single ES5 file (inline sourcemaps for debugging)
   - Validates the bundle exports a `middleware` function
   - Registers the bundled code with its domain list and options
3. Gateway starts the TCP listener and health endpoint

```yaml
# plugins.yaml (generated by CLI at `agent-sandbox generate`)
plugins:
  - name: github-pat
    domains: ["api.github.com", "*.github.com"]
    options:
      token: "ghp_..."
    src: /plugins/github-pat/src/middleware.ts

  - name: mcp-oauth
    domains: ["*.modelcontextprotocol.io"]
    options:
      client_id: "..."
      client_secret: "..."
    src: /plugins/mcp-oauth/src/middleware.ts
```

### Plugin Loader Architecture

```
                  startup
                     │
         ┌───────────────────────┐
         │  Read plugins.yaml    │
         └───────────┬───────────┘
                     │
         ┌───────────▼───────────┐
         │  For each plugin:     │
         │  esbuild bundle TS→JS │
         └───────────┬───────────┘
                     │
         ┌───────────▼───────────┐
         │  Register plugin:     │
         │  domains + bundle +   │
         │  options              │
         └───────────┬───────────┘
                     │
         ┌───────────▼───────────┐
         │  Start proxy listener │
         │  + health mux (:8080) │
         └───────────────────────┘
```

### Middleware Execution (per-request)

When a MITM'd request arrives and matches a plugin's domains:

1. A **fresh goja VM** is created for the request (isolation — no shared state between requests)
2. **Host APIs** are injected into the VM's global scope:
   - `request.getHeader(name)` / `request.setHeader(name, value)` / `request.deleteHeader(name)`
   - `request.url`, `request.method`, `request.host`
   - `options` — the plugin's resolved options from `plugins.yaml`
   - `log.info(msg)` / `log.warn(msg)` / `log.error(msg)` — structured logging via slog
   - `secrets.register(value)` — registers a value for log redaction
3. The bundled JS is executed — the exported `middleware(ctx)` function is called
4. Header mutations made via host APIs are applied to the actual `*http.Request`
5. The VM is discarded (GC'd)

This per-request VM model ensures plugins cannot leak state or interfere with each other, at the cost of ~0.1ms per invocation (acceptable for proxy latency budgets).

### Route Handlers (health mux on :8080)

The gateway serves non-proxy HTTP routes on `:8080` using Go's standard `http.ServeMux`:

- `GET /health` — returns 200 when the proxy is ready (used by agent entrypoint to gate iptables setup)
- `GET /ready` — returns 200 after all plugins are loaded and bundled
- `GET /plugins` — returns JSON list of loaded plugins with their domain registrations (debug endpoint)

The agent entrypoint script polls `/health` before activating iptables DNAT, ensuring no traffic is lost during gateway startup.

### auth_headers Config (Native — No Generated Code)

Simple header injection no longer generates `.go` files. Instead, `gateway.services[].headers` from `agent.yaml` are translated directly into plugin entries in `plugins.yaml` at generate time:

```yaml
# agent.yaml
gateway:
  services:
    - url: https://api.example.com
      headers:
        Authorization: "Bearer ${MY_API_KEY}"
```

The CLI resolves `${MY_API_KEY}` from the environment and emits:

```yaml
# plugins.yaml (generated)
plugins:
  - name: _auth_api.example.com
    domains: ["api.example.com"]
    options:
      headers:
        Authorization: "Bearer sk-actual-value..."
    src: /gateway/builtins/auth-headers.ts
```

The built-in `auth-headers.ts` plugin is bundled with the gateway binary and simply applies `options.headers` to each request. No code generation, no Go templates — just data flowing through the same plugin system.

## Connection Flow

1. Agent makes outbound TCP connection (e.g., `curl https://github.com`)
2. iptables DNAT rule redirects port 443 → gateway:8443
3. Gateway's TCP listener accepts the connection
4. Reads first 4096 bytes — expects a TLS ClientHello
5. Calls `extractSNI()` to parse the server name from the ClientHello
6. Iterates registered `RequestHandler`s, calling `Matches(serverName)`
7. **Match found** → delegates to handler (currently only `mitm.Handler`)
8. **No match** → passthrough: dials `serverName:443`, replays the ClientHello bytes, then pipes bidirectionally (`io.Copy` both directions with half-close)

The passthrough path preserves end-to-end TLS — the gateway never sees plaintext for non-MITM'd hosts.

## DNS

The gateway includes a DNS forwarder on port 53, but in the current v1 architecture DNS resolution uses Docker's built-in DNS. The agent's `/etc/resolv.conf` points to Docker's embedded DNS server (127.0.0.11), which resolves container names (e.g., `gateway`) automatically.

The gateway DNS server exists as infrastructure for future use cases (DNS-based routing, filtering) but is not required for the transparent proxy to function.

## MITM Pipeline

When a connection matches a MITM domain, `mitm.Handler` takes over:

1. **Certificate generation** — At startup, the gateway generates an ECDSA P-256 CA keypair and writes the CA cert to a shared volume (`/shared/certs/ca.crt`). Per-domain leaf certs are generated on demand via `CertCache.GetOrCreate(domain, caCert)` (thread-safe, double-checked locking).
2. **TLS handshake** — wraps the client connection in a `prefixConn` (replays the already-read ClientHello bytes), then performs a `tls.Server` handshake using the generated cert.
3. **HTTP request loop** (keep-alive aware):
   - `http.ReadRequest` from the decrypted stream
   - Call `applyMiddleware(req)` — finds matching middleware by domain, executes in order
   - Forward the modified request to real upstream via fresh TLS connection
   - Write upstream response back to agent over the MITM'd connection
   - Repeat until `Connection: close` or EOF

The agent's TLS client trusts the gateway CA (installed at container startup via `update-ca-certificates` from the shared certs volume), so the MITM is transparent.

## Log Redaction

Gateway logs are protected with two layers via the `redact.Handler` (wraps `slog`):

1. **Key-based** — attributes named `token`, `authorization`, `api_key`, etc. are always redacted
2. **Value-based** — all string attribute values are scanned for known secret substrings (collected from middleware via `gateway.RegisterSecret()` at startup) and replaced with `[REDACTED]`

This prevents credentials from leaking into container logs even if a handler inadvertently logs request details.

## Adding New Plugins

All plugins follow the same pattern: a directory with `plugin.yaml` (declares domains and options schema) and `src/middleware.ts` (implements the request modification logic). The gateway loads them at startup — no recompilation needed.

### As a feature plugin (recommended)

1. Create a directory under `core/plugins/<name>/`
2. Write `plugin.yaml`:

```yaml
name: my-plugin
description: Injects API key for example.com
contributes:
  gateway:
    services:
      - domains: ["api.example.com"]
        options_schema:
          api_key:
            type: string
            source: env
```

3. Write `src/middleware.ts`:

```typescript
import type { MiddlewareContext } from "@agent-sandbox/sdk";

export function middleware(ctx: MiddlewareContext): void {
  const key = ctx.options.api_key;
  secrets.register(key);
  ctx.request.setHeader("Authorization", `Bearer ${key}`);
}
```

4. Reference the plugin in `agent.yaml`:

```yaml
plugins:
  - name: my-plugin
    options:
      api_key: ${MY_API_KEY}
```

### Via gateway.services headers (simple header injection)

For straightforward header injection, just declare headers in `agent.yaml`:

```yaml
gateway:
  services:
    - url: https://api.example.com
      headers:
        Authorization: "Bearer ${MY_API_KEY}"
```

The CLI resolves env vars and emits a plugin entry using the built-in `auth-headers.ts` handler. Template variables supported in header values:
- `${value}` — raw env var value
- `${base64_basic}` — base64("x-access-token:\<value\>") for git HTTP auth

### Design principles

- No compile-time code generation — plugins are TypeScript loaded at runtime
- The gateway container needs no env var passthrough — secrets are baked into `plugins.yaml` options
- Per-request VM isolation — plugins cannot leak state or interfere with each other
- Same plugin system for all middleware — auth headers, OAuth, custom logic all use the same TS interface
