// Package sandbox is the root module for the agent-sandbox CLI.
// Core assets (gateway, plugins, templates) are fetched at runtime from GitHub Releases
// rather than embedded in the binary.
package sandbox
