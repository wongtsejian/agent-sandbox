# Minimal agent config for sandbox integration tests.
# Uses gateway services to trigger MITM proxy mode.
name: sandbox-test
log_level: debug
runtime:
  image: "@builtin/codex"
  entrypoint: ["sleep", "infinity"]
gateway:
  services:
    - url: https://httpbin.org
      headers:
        Authorization: Bearer ${TEST_SECRET}
