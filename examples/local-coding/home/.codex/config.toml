# --- codex-switch:begin ---
model = "claude-sonnet-4.6"
model_provider = "agent_gateway_kiro"
# --- codex-switch:end ---

model_catalog_json = "/home/agent/.codex/models.json"
sandbox = "none"

[model_providers.agent_gateway_kiro]
name = "Agent Gateway (Kiro)"
base_url = "https://agent-gateway.stx-ai.net/kiro/v1"
http_headers = { Authorization = "Bearer dummy" }
wire_api = "responses"

[model_providers.agent_gateway_codex]
name = "Agent Gateway (Codex)"
base_url = "https://agent-gateway.stx-ai.net/codex/v1"
http_headers = { Authorization = "Bearer dummy" }
wire_api = "responses"

[mcp_servers.notion]
url = "https://mcp.notion.com/mcp"
bearer_token_env_var = "MCP_NOTION_TOKEN"
