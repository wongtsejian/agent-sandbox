# yaml-language-server: $schema=../.build/reviewer/schema.json
name: reviewer
runtime: codex

features:
  - plugin: telegram
    bot_token: ${REVIEWER_TELEGRAM_BOT_TOKEN}
    access_control:
      allowed_users: ["@your_username"]
