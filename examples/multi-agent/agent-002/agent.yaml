# yaml-language-server: $schema=../.build/schema.json
name: agent-002
core_version: latest
runtime:
  image: "@builtin/claude-code"
installations:
  - plugin: "@builtin/home-override"
    options:
      home_directory: "./home"
      volume: true
