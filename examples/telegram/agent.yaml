# yaml-language-server: $schema=.build/schema.json
name: coder
runtime: codex
log_level: debug
features:
  telegram:
    access_control:
      allowed_users: ["@${TELEGRAM_USERNAME}"]
      require_mention: false
